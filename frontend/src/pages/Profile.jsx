import { useState } from 'react'; import { Save } from 'lucide-react'; import { useAuth } from '../context/AuthContext'; import api from '../services/api';
export default function Profile() { const { user } = useAuth(); const [form, setForm] = useState({ full_name: user.full_name || '', phone: user.phone || '', date_of_birth: user.date_of_birth || '', gender: user.gender || '' }); const [saved, setSaved] = useState(false); const update = (e) => setForm({ ...form, [e.target.name]: e.target.value }); const save = async (e) => { e.preventDefault(); await api.put('/users/me', form); setSaved(true); }; return <section className="page"><span className="eyebrow">YOUR DETAILS</span><h2>Profile & settings</h2><p className="muted page-lead">Keep your contact details ready for reminders.</p><form className="form-panel narrow" onSubmit={save}><label>Full name<input name="full_name" value={form.full_name} onChange={update} /></label><label>Email<input value={user.email} disabled /></label><label>Phone<input name="phone" value={form.phone} onChange={update} /></label><label>Date of birth<input type="date" name="date_of_birth" value={form.date_of_birth} onChange={update} /></label><label>Gender<input name="gender" value={form.gender} onChange={update} placeholder="Optional" /></label><div className="form-actions"><span className="muted">{saved ? 'Saved successfully.' : ''}</span><button className="primary"><Save size={16} /> Save changes</button></div></form></section> }
