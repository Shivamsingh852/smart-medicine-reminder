import { useEffect, useState } from 'react'; import api from '../services/api';
export default function History() { const [rows, setRows] = useState([]); useEffect(() => { api.get('/doses/history?days=30').then(({ data }) => setRows(data.history)); }, []); return <section className="page"><span className="eyebrow">YOUR RECORD</span><h2>Reminder history</h2><p className="muted page-lead">A simple view of the last 30 days.</p><div className="table-wrap"><table><thead><tr><th>Medicine</th><th>Scheduled</th><th>Actual</th><th>Status</th><th>Date</th></tr></thead><tbody>{rows.map((row) => <tr key={row.id}><td><strong>{row.medicine_name}</strong></td><td>{row.scheduled_time}</td><td>{row.actual_time ? new Date(row.actual_time).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' }) : '—'}</td><td><span className={`pill status-${row.status}`}>{row.status}</span></td><td>{new Date(row.created_at).toLocaleDateString()}</td></tr>)}</tbody></table>{!rows.length && <div className="empty-state">Dose activity will appear here after your first check-in.</div>}</div></section> }
